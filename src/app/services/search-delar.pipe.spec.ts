import { SearchDelarPipe } from './search-delar.pipe';

describe('SearchDelarPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDelarPipe();
    expect(pipe).toBeTruthy();
  });
});
